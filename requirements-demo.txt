# LyricMood demo dependencies for Streamlit Community Cloud.
#
# SCC runs `streamlit run demo_entry.py` against the repo-root requirements.txt,
# which on the `demo` branch is a copy of THIS file (see docs/DEPLOY_DEMO.md §4).
# The demo runs the API in-process, so it needs BOTH the API and UI deps, plus
# huggingface_hub to download the model bundle at first boot.
#
# Pins are copied verbatim from requirements-api.txt + requirements-ui.txt — keep
# them in sync with those source files. Regenerate the `demo` branch whenever the
# pins here change.

# --- from requirements-api.txt ---
fastapi>=0.110
uvicorn[standard]>=0.29
pydantic>=2.10
pydantic-settings>=2.2
qdrant-client>=1.10,<2
structlog>=24.1
joblib>=1.2
scikit-learn>=1.3
pandas>=2.0
numpy>=1.24
shap>=0.44
matplotlib>=3.7
onnxruntime>=1.17
tokenizers>=0.15
slowapi>=0.1.9,<0.2
prometheus-client>=0.20

# --- from requirements-ui.txt ---
streamlit>=1.30
httpx>=0.27

# --- demo-only: bundle download ---
huggingface_hub>=0.23
